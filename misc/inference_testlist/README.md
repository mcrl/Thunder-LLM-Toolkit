# Usage

## Running tests that only runs on cuda

```bash
cd /path/to/thunder-llm
bash misc/inference_testlist/run_tests.sh cuda
```

## Running all tests, including cpu test

```bash
cd /path/to/thunder-llm
bash misc/inference_testlist/run_tests.sh
```